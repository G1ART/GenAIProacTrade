# Explanation prototype — `BBY` under `hyp_pit_join_key_mismatch_as_of_boundary_v1`

## What changed (observable)

- Substrate audit classifies **BBY** as **`state_change_built_but_join_key_mismatch`** with signal date **2025-12-08**.
- Referenced state_change grid’s earliest as_of in the audit run is **2026-09-28**, yielding **`earliest_state_change_as_of_after_signal_no_pit_match`** under the default join rule.

## Why it may matter (judgment, not advice)

- For a subset of joined recipe rows, state_change scores exist but the earliest as_of in the referenced run is after the signal_available_date; joins fail under current PIT keys while economic content may still be coherent under alternate as-of or lag conventions.

## Mechanism under test

- Replaying join logic under alternate state_change run selection or explicit lag windows will reclassify some rows from join_key_mismatch to joined or to a documented exclusion bucket.

## Evidence supporting the framing

- Deterministic audit fields above are reproduced from the **PIT-lab fixture** (Phase 36.1 residual set), not a model guess.
- Hypothesis **Residual no_state_change_join (join_key_mismatch) reflects score-run as-of vs signal-date ordering, not missing SC build** ties the residual to **as-of / run reference** rather than “missing data” narratives.

## What remains uncertain

- Alternate **PIT run specs** (explicit in the experiment scaffold) are **not yet DB-executed** — this text is a **prototype surface** only.
- Economic interpretation of the signal row is **out of scope** until the PIT harness compares alternate boundaries without leakage.

## Why this is not a black-box recommendation

- No score or action is produced here; the object is **transparent classification** + **falsifiable hypotheses** + **deferred adversarial review** items.
- Promotion to any user-facing “signal” would require **promotion gate** artifacts not auto-generated by this sprint.

## Falsifiers (hypothesis)

- All eight rows remain join_key_mismatch under every deterministic alternate run spec in the PIT harness.
- Alternate boundaries introduce lookahead or non-PIT leakage per audit rules.
